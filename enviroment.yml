# conda env create --file environment.yml
# mamba env create --file environment.yml
name: eda_pereira_info
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - numpy
  - pandas
  - matplotlib
  - pip
  - scikit-learn
  - seaborn
  